org.gradle.jvmargs=-Xmx3g -Dfile.encoding=UTF-8
org.gradle.configuration-cache=true
org.gradle.parallel=true
# Task outputs survive between builds, and on CI between runs: `setup-gradle` saves the local
# cache directory, so a run only recompiles the modules whose inputs actually moved.
org.gradle.caching=true
kotlin.code.style=official
